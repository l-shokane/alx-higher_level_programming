#!/usr/bin/node
// A script that prints 3 lines using an array of strings and loops.

const myLangugages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (const language of myLangugages) {
  console.log(language);
}
